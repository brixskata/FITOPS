import { CreditCard } from 'lucide-react'
import { payments } from '../memberData'
import { PageIntro } from '../components/MemberUI'

export default function Payments() { return <div className="space-y-8"><PageIntro eyebrow="My account" title="Payments" description="View your membership payment history and receipts." /><div className="rounded-2xl border border-ink/10 bg-white shadow-[0_16px_50px_rgba(18,18,18,0.04)]"><div className="flex items-center gap-3 border-b border-ink/10 p-6"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-accent"><CreditCard size={19} /></span><div><h3 className="font-heading text-3xl uppercase text-ink">Payment history</h3><p className="mt-1 text-xs text-ink/45">Your recent FitOps transactions.</p></div></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left"><thead className="bg-ink/[0.03] text-[11px] uppercase tracking-[0.16em] text-ink/40"><tr><th className="px-6 py-4 font-semibold">Date</th><th className="px-6 py-4 font-semibold">Description</th><th className="px-6 py-4 font-semibold">Method</th><th className="px-6 py-4 font-semibold">Amount</th><th className="px-6 py-4 font-semibold">Status</th></tr></thead><tbody className="divide-y divide-ink/10">{payments.map((payment) => <tr key={payment.date} className="text-sm"><td className="px-6 py-4 font-semibold text-ink">{payment.date}</td><td className="px-6 py-4 text-ink/60">{payment.description}</td><td className="px-6 py-4 text-ink/55">{payment.method}</td><td className="px-6 py-4 font-semibold text-ink">{payment.amount}</td><td className="px-6 py-4"><span className="rounded-full bg-accent/20 px-3 py-1 text-xs font-bold text-ink">{payment.status}</span></td></tr>)}</tbody></table></div></div></div> }
