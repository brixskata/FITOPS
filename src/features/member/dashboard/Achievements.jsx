import { motion } from 'framer-motion'
import DashboardIcon from './DashboardIcons'
import { dashboardAchievements } from './DashboardData'
import { Reveal, Stagger, staggerItem } from './Motion'

export default function Achievements() { return <Reveal as="section" className="rounded-2xl border border-ink/10 bg-white p-6 shadow-[0_16px_50px_rgba(18,18,18,0.04)] sm:p-7"><div className="flex items-end justify-between gap-4"><div><p className="section-kicker text-ink/45">Keep showing up</p><h2 className="mt-2 font-heading text-3xl uppercase text-ink">Achievements</h2></div><span className="text-xs font-semibold text-ink/55">3 of 4 unlocked</span></div><Stagger className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4">{dashboardAchievements.map((item) => <motion.div key={item.label} variants={staggerItem} whileHover={item.unlocked ? { y: -4, scale: 1.02, boxShadow: '0 16px 35px rgba(255,230,0,0.22)' } : { opacity: 0.82 }} transition={{ duration: 0.3 }} className={`rounded-2xl border p-4 text-center ${item.unlocked ? 'border-accent/50 bg-accent/10' : 'border-ink/10 bg-ink/[0.02] grayscale opacity-60'}`}><motion.span whileHover={item.unlocked ? { rotate: 6, scale: 1.05 } : { filter: 'grayscale(0)' }} transition={{ duration: 0.3 }} className={`mx-auto flex h-12 w-12 items-center justify-center rounded-full ${item.unlocked ? 'bg-accent text-ink' : 'bg-ink/10 text-ink/45'}`}><DashboardIcon name={item.icon} size={21} /></motion.span><p className="mt-4 text-xs font-bold text-ink">{item.label}</p><p className="mt-1 text-[10px] leading-4 text-ink/55">{item.detail}</p></motion.div>)}</Stagger></Reveal> }
