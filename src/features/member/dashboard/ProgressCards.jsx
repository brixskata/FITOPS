import DashboardIcon from './DashboardIcons'
import { dashboardProgress } from './DashboardData'
import { AnimatedNumber, Stagger, staggerItem } from './Motion'
import { motion } from 'framer-motion'

export default function ProgressCards() { return <section><div><p className="section-kicker text-ink/45">Your momentum</p><h2 className="mt-2 font-heading text-3xl uppercase text-ink">Fitness progress</h2></div><Stagger className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{dashboardProgress.map((item) => <motion.div key={item.label} variants={staggerItem} whileHover={{ y: -4, boxShadow: '0 20px 55px rgba(18,18,18,0.08)' }} transition={{ duration: 0.3 }} className="rounded-2xl border border-ink/10 bg-white p-5 shadow-[0_16px_50px_rgba(18,18,18,0.04)]"><div className="flex items-start justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-accent/15 text-ink"><DashboardIcon name={item.icon} size={19} /></span><span className="text-[10px] font-bold uppercase tracking-wider text-ink/40">{item.progress ? `${item.progress}%` : 'All time'}</span></div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.14em] text-ink/45">{item.label}</p><p className="mt-1 text-2xl font-bold text-ink"><AnimatedNumber value={item.counter} decimals={item.decimals} suffix={item.suffix} /></p><p className="mt-1 text-xs text-ink/55">{item.detail}</p>{item.progress && <div className="mt-5 h-1.5 overflow-hidden rounded-full bg-ink/8"><motion.div initial={{ width: 0 }} whileInView={{ width: `${item.progress}%` }} viewport={{ once: true }} transition={{ duration: 0.7, ease: 'easeOut' }} className="h-full rounded-full bg-accent" /></div>}</motion.div>)}</Stagger></section> }
