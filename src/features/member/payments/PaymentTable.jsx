import { motion } from 'framer-motion'
import { ArrowUpRight, CreditCard } from 'lucide-react'
import { formatPaymentAmount, formatPaymentDate, humanizePaymentLabel, paymentStatusClass } from './paymentUtils'
function StatusBadge({ status }) { return <span className={`inline-flex w-fit rounded-full border px-3 py-1 text-[10px] font-bold uppercase tracking-[0.15em] ${paymentStatusClass(status)}`}>{humanizePaymentLabel(status)}</span> }
function ViewButton({ onClick }) { return <button type="button" onClick={onClick} aria-label="View payment details" className="inline-flex items-center gap-2 rounded-xl border border-ink/10 px-3 py-2 text-xs font-bold text-ink transition hover:border-accent hover:bg-accent/10">View <ArrowUpRight size={14} /></button> }
export default function PaymentTable({ payments, onView }) {
  return <div className="overflow-hidden rounded-2xl border border-ink/10 bg-white shadow-[0_16px_50px_rgba(18,18,18,0.04)]"><div className="flex items-center gap-3 border-b border-ink/10 p-5 sm:p-6"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-accent text-ink"><CreditCard size={19} /></span><div><h2 className="font-heading text-2xl uppercase tracking-wide text-ink sm:text-3xl">Payment history</h2><p className="mt-1 text-xs text-ink/45">Your recorded FitOps transactions.</p></div></div><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[900px] text-left"><thead className="bg-ink/[0.03] text-[11px] uppercase tracking-[0.16em] text-ink/40"><tr><th className="px-6 py-4 font-semibold">Date</th><th className="px-6 py-4 font-semibold">Receipt Number</th><th className="px-6 py-4 font-semibold">Membership</th><th className="px-6 py-4 font-semibold">Method</th><th className="px-6 py-4 font-semibold">Amount</th><th className="px-6 py-4 font-semibold">Status</th><th className="px-6 py-4 text-right font-semibold">Action</th></tr></thead><tbody className="divide-y divide-ink/10">{payments.map((payment, index) => <motion.tr key={payment.id} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: Math.min(index * 0.035, 0.2) }} className="text-sm transition hover:bg-ink/[0.02]"><td className="whitespace-nowrap px-6 py-4 font-semibold text-ink">{formatPaymentDate(payment.paid_at)}</td><td className="px-6 py-4 font-mono text-xs font-semibold text-ink">{payment.receipt_number || '—'}</td><td className="px-6 py-4"><p className="font-semibold text-ink">{payment.membership?.plan_name || 'Membership'}</p><p className="mt-1 text-xs text-ink/45">{payment.membership?.membership_number || '—'}</p></td><td className="px-6 py-4 text-ink/60">{humanizePaymentLabel(payment.payment_method)}</td><td className="whitespace-nowrap px-6 py-4 font-semibold text-ink">{formatPaymentAmount(payment.amount)}</td><td className="px-6 py-4"><StatusBadge status={payment.status} /></td><td className="px-6 py-4 text-right"><ViewButton onClick={() => onView(payment.id)} /></td></motion.tr>)}</tbody></table></div><div className="divide-y divide-ink/10 md:hidden">{payments.map((payment, index) => <motion.article key={payment.id} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: Math.min(index * 0.04, 0.2) }} className="p-5"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-sm font-bold text-ink">{formatPaymentDate(payment.paid_at)}</p><p className="mt-1 truncate font-mono text-[11px] text-ink/45">{payment.receipt_number || 'Receipt unavailable'}</p></div><StatusBadge status={payment.status} /></div><div className="mt-4 rounded-xl bg-ink/[0.025] p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-sm font-semibold text-ink">{payment.membership?.plan_name || 'Membership'}</p><p className="mt-1 text-xs text-ink/45">{payment.membership?.membership_number || 'Membership number unavailable'}</p></div><p className="whitespace-nowrap text-sm font-bold text-ink">{formatPaymentAmount(payment.amount)}</p></div><p className="mt-3 text-xs text-ink/55">{humanizePaymentLabel(payment.payment_method)}</p></div><div className="mt-4"><ViewButton onClick={() => onView(payment.id)} /></div></motion.article>)}</div></div>
}
