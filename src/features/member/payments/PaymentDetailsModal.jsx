import { useEffect, useRef } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { CalendarDays, CreditCard, RefreshCw, X } from 'lucide-react'
import { formatPaymentAmount, formatPaymentDate, formatPaymentDateTime, humanizePaymentLabel, paymentStatusClass } from './paymentUtils'

export default function PaymentDetailsModal({ open, loading, error, payment, onClose, onRetry }) {
  const dialogRef = useRef(null); const closeButtonRef = useRef(null)
  useEffect(() => { if (!open) return undefined; const previousOverflow = document.body.style.overflow; const previousFocus = document.activeElement; document.body.style.overflow = 'hidden'; closeButtonRef.current?.focus(); const handleKeyDown = (event) => { if (event.key === 'Escape') onClose(); if (event.key !== 'Tab') return; const focusable = dialogRef.current?.querySelectorAll('button:not([disabled]), [href], [tabindex]:not([tabindex="-1"])'); if (!focusable?.length) return; const first = focusable[0]; const last = focusable[focusable.length - 1]; if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus() } if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus() } }; window.addEventListener('keydown', handleKeyDown); return () => { document.body.style.overflow = previousOverflow; window.removeEventListener('keydown', handleKeyDown); previousFocus?.focus?.() } }, [onClose, open])
  return <AnimatePresence>{open && <motion.div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/60 p-3 backdrop-blur-sm sm:p-5" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.div ref={dialogRef} role="dialog" aria-modal="true" aria-labelledby="member-payment-details-title" initial={{ opacity: 0, y: 18, scale: 0.985 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 10, scale: 0.985 }} className="flex max-h-[90vh] w-full max-w-2xl flex-col overflow-hidden rounded-2xl bg-white shadow-2xl sm:rounded-3xl"><header className="flex shrink-0 items-start justify-between border-b border-ink/10 px-5 py-5 sm:px-7"><div><p className="text-[10px] font-bold uppercase tracking-[0.22em] text-ink/40">Payment record</p><h2 id="member-payment-details-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">Details</h2></div><button ref={closeButtonRef} type="button" onClick={onClose} aria-label="Close payment details" className="inline-flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-ink/10 text-ink/55 transition hover:border-accent hover:text-ink"><X size={18} /></button></header><div className="min-h-0 overflow-y-auto px-5 py-6 sm:px-7">{loading ? <div className="flex min-h-56 items-center justify-center text-sm text-ink/45"><CreditCard className="mr-3 animate-pulse" size={19} />Loading payment details...</div> : error ? <div className="flex min-h-56 items-center justify-center"><div className="max-w-sm text-center"><p className="text-sm leading-6 text-ink/60">{error}</p><button type="button" onClick={onRetry} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-ink px-4 py-2.5 text-xs font-bold uppercase tracking-wider text-accent"><RefreshCw size={15} /> Retry</button></div></div> : payment && <><div className="flex flex-col gap-4 rounded-2xl bg-ink p-5 text-white sm:flex-row sm:items-center sm:justify-between"><div><p className="font-mono text-sm font-semibold">{payment.receipt_number || 'Receipt unavailable'}</p><p className="mt-2 text-2xl font-bold">{formatPaymentAmount(payment.amount)}</p></div><span className={`w-fit rounded-full border px-3 py-1 text-[10px] font-bold uppercase tracking-[0.16em] ${paymentStatusClass(payment.status)}`}>{humanizePaymentLabel(payment.status)}</span></div><dl className="mt-5 grid gap-4 rounded-2xl border border-ink/10 bg-ink/[0.02] p-5 sm:grid-cols-2"><div><dt className="text-xs text-ink/40">Payment method</dt><dd className="mt-1 text-sm font-semibold text-ink">{humanizePaymentLabel(payment.payment_method)}</dd></div><div><dt className="text-xs text-ink/40">Paid date</dt><dd className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><CalendarDays size={15} className="text-ink/40" />{formatPaymentDateTime(payment.paid_at)}</dd></div><div><dt className="text-xs text-ink/40">Reference number</dt><dd className="mt-1 break-words text-sm font-semibold text-ink">{payment.reference_number || '—'}</dd></div><div><dt className="text-xs text-ink/40">Created</dt><dd className="mt-1 text-sm font-semibold text-ink">{formatPaymentDateTime(payment.created_at)}</dd></div></dl><section className="mt-5 rounded-2xl border border-ink/10 p-5"><p className="text-xs font-bold uppercase tracking-[0.18em] text-ink/40">Membership</p><dl className="mt-4 grid gap-4 sm:grid-cols-2"><div><dt className="text-xs text-ink/40">Plan</dt><dd className="mt-1 text-sm font-semibold text-ink">{payment.membership?.plan_name || '—'}</dd></div><div><dt className="text-xs text-ink/40">Membership number</dt><dd className="mt-1 break-words font-mono text-xs font-semibold text-ink">{payment.membership?.membership_number || '—'}</dd></div><div><dt className="text-xs text-ink/40">Status</dt><dd className="mt-1 text-sm font-semibold capitalize text-ink">{payment.membership?.status || '—'}</dd></div><div><dt className="text-xs text-ink/40">Start date</dt><dd className="mt-1 text-sm font-semibold text-ink">{formatPaymentDate(payment.membership?.starts_at)}</dd></div><div><dt className="text-xs text-ink/40">End date</dt><dd className="mt-1 text-sm font-semibold text-ink">{formatPaymentDate(payment.membership?.ends_at)}</dd></div></dl></section></>}</div><footer className="shrink-0 border-t border-ink/10 bg-white px-5 py-4 text-right sm:px-7"><button type="button" onClick={onClose} className="w-full rounded-xl bg-ink px-5 py-3 text-sm font-bold text-accent transition hover:-translate-y-0.5 sm:w-auto">Close</button></footer></motion.div></motion.div>}</AnimatePresence>
}
