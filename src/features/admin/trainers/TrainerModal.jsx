import { useEffect, useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { Eye, EyeOff, LoaderCircle, X } from 'lucide-react'
import Button from '../../../components/common/Button'

const fields = [
  { name: 'name', label: 'Full Name', type: 'text', required: true },
  { name: 'email', label: 'Email', type: 'email', required: true },
  { name: 'employee_code', label: 'Employee Code', type: 'text', readOnly: true },
  { name: 'specialization', label: 'Specialization', type: 'text' },
  { name: 'experience_years', label: 'Experience Years', type: 'number', min: 0 },
  { name: 'hire_date', label: 'Hire Date', type: 'date' },
]

function Field({ field, value, error, onChange }) {
  return (
    <label className="block">
      <span className="mb-2 block text-sm font-medium text-ink/70">{field.label}{field.required && <span className="text-rose-600"> *</span>}</span>
      <input name={field.name} type={field.type} min={field.min} value={value ?? ''} onChange={onChange} readOnly={field.readOnly} disabled={field.readOnly} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent read-only:cursor-not-allowed read-only:bg-ink/[0.04] disabled:cursor-not-allowed disabled:bg-ink/[0.04]" />
      {field.readOnly && <p className="mt-2 text-xs text-ink/45">Automatically generated by FitOps.</p>}
      {error && <p className="mt-2 text-xs font-medium text-rose-600">{error[0]}</p>}
    </label>
  )
}

export default function TrainerModal({ open, mode, loading, form, errors, message, saving, onClose, onSubmit, onChange }) {
  const isEditing = mode === 'edit'
  const [showPassword, setShowPassword] = useState(false)
  const [showPasswordConfirmation, setShowPasswordConfirmation] = useState(false)

  useEffect(() => {
    if (!open) return undefined
    const previousOverflow = document.body.style.overflow
    document.body.style.overflow = 'hidden'
    setShowPassword(false)
    setShowPasswordConfirmation(false)
    return () => { document.body.style.overflow = previousOverflow }
  }, [open, mode])

  return (
    <AnimatePresence>
      {open && (
        <motion.div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/60 p-4 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}>
          <motion.div role="dialog" aria-modal="true" aria-labelledby="trainer-modal-title" initial={{ opacity: 0, y: 18, scale: 0.98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 10 }} transition={{ duration: 0.25 }} className="flex max-h-[90vh] w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl">
            <div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8">
              <div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Admin workspace</p><h2 id="trainer-modal-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">{isEditing ? 'Edit Trainer' : 'Add Trainer'}</h2></div>
              <button type="button" onClick={onClose} aria-label="Close Trainer modal" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button>
            </div>
            <div className="min-h-0 overflow-y-auto px-6 py-6 sm:px-8">
              {message && <div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">{message}</div>}
              {loading ? (
                <div className="flex min-h-56 items-center justify-center text-ink/45"><LoaderCircle className="animate-spin" size={24} /></div>
              ) : (
                <form id="trainer-form" onSubmit={onSubmit} className="space-y-5">
                  <div className="grid gap-4 sm:grid-cols-2">{fields.map((field) => <Field key={field.name} field={field} value={form[field.name]} error={errors[field.name]} onChange={onChange} />)}</div>
                  <div className="grid gap-4 sm:grid-cols-2">
                    <label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Status <span className="text-rose-600">*</span></span><select name="status" value={form.status ?? 'active'} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent"><option value="active">Active</option><option value="inactive">Inactive</option></select>{errors.status && <p className="mt-2 text-xs font-medium text-rose-600">{errors.status[0]}</p>}</label>
                    <div />
                  </div>
                  <label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Biography</span><textarea name="biography" rows="4" value={form.biography ?? ''} onChange={onChange} className="w-full resize-y rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.bio && <p className="mt-2 text-xs font-medium text-rose-600">{errors.bio[0]}</p>}</label>
                  <div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Password{isEditing && ' (leave blank to keep current)'}</span><div className="relative"><input name="password" type={showPassword ? 'text' : 'password'} value={form.password ?? ''} onChange={onChange} autoComplete="new-password" className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 pr-12 text-sm text-ink outline-none transition focus:border-accent" /><button type="button" onClick={() => setShowPassword((current) => !current)} aria-label={showPassword ? 'Hide password' : 'Show password'} className="absolute right-2 top-1/2 inline-flex h-9 w-9 -translate-y-1/2 items-center justify-center rounded-xl text-ink/45 transition hover:bg-ink/5 hover:text-ink focus:outline-none focus:ring-2 focus:ring-accent">{showPassword ? <EyeOff size={17} /> : <Eye size={17} />}</button></div>{errors.password && <p className="mt-2 text-xs font-medium text-rose-600">{errors.password[0]}</p>}</label><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Confirm Password</span><div className="relative"><input name="password_confirmation" type={showPasswordConfirmation ? 'text' : 'password'} value={form.password_confirmation ?? ''} onChange={onChange} autoComplete="new-password" className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 pr-12 text-sm text-ink outline-none transition focus:border-accent" /><button type="button" onClick={() => setShowPasswordConfirmation((current) => !current)} aria-label={showPasswordConfirmation ? 'Hide password confirmation' : 'Show password confirmation'} className="absolute right-2 top-1/2 inline-flex h-9 w-9 -translate-y-1/2 items-center justify-center rounded-xl text-ink/45 transition hover:bg-ink/5 hover:text-ink focus:outline-none focus:ring-2 focus:ring-accent">{showPasswordConfirmation ? <EyeOff size={17} /> : <Eye size={17} />}</button></div>{errors.password_confirmation && <p className="mt-2 text-xs font-medium text-rose-600">{errors.password_confirmation[0]}</p>}</label></div>
                </form>
              )}
            </div>
            <div className="flex shrink-0 flex-col-reverse gap-3 border-t border-ink/10 bg-white px-6 py-4 sm:flex-row sm:justify-end sm:px-8"><Button type="button" onClick={onClose} disabled={saving} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Cancel</Button><Button type="submit" form="trainer-form" disabled={saving || loading} className="w-full sm:w-auto">{saving ? (isEditing ? 'Updating...' : 'Saving...') : isEditing ? 'Update Trainer' : 'Save Trainer'}</Button></div>
          </motion.div>
        </motion.div>
      )}
    </AnimatePresence>
  )
}
