import AdminModal from '../../../components/common/AdminModal'
import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { CalendarDays, Mail, UsersRound, X } from 'lucide-react'
import Button from '../../../components/common/Button'

const display = (value) => value === null || value === undefined || value === '' ? '—' : value
const formatDate = (date) => date ? new Date(`${date}T00:00:00`).toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' }) : '—'

export default function TrainerDetailsModal({ open, loading, error, trainer, onClose }) {
  useEffect(() => {
    if (!open) return undefined
    const previousOverflow = document.body.style.overflow
    document.body.style.overflow = 'hidden'
    return () => { document.body.style.overflow = previousOverflow }
  }, [open])

  return <AnimatePresence>{open && <AdminModal><motion.div role="dialog" aria-modal="true" aria-labelledby="trainer-details-title" initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 10 }} className="flex w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Trainer profile</p><h2 id="trainer-details-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">Details</h2></div><button type="button" onClick={onClose} aria-label="Close Trainer details" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button></div><div className="min-h-0 overflow-y-auto px-6 py-6 sm:px-8">{loading ? <div className="flex min-h-60 items-center justify-center text-ink/45">Loading Trainer details...</div> : error ? <div className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">{error}</div> : trainer && <><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-ink text-xl font-bold text-accent">{trainer.name?.split(' ').map((part) => part[0]).join('').slice(0, 2).toUpperCase() || 'T'}</div><div><h3 className="text-xl font-bold text-ink">{display(trainer.name)}</h3><p className="mt-1 text-sm text-ink/55">{display(trainer.specialization)}</p></div><span className="inline-flex w-fit rounded-full border border-ink/10 bg-ink/5 px-3 py-1 text-xs font-bold uppercase tracking-[0.2em] text-ink/65 sm:ml-auto">{display(trainer.status)}</span></div><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-xs text-ink/45">Email</p><p className="mt-1 flex items-center gap-2 break-all text-sm font-semibold text-ink"><Mail size={15} className="shrink-0 text-ink/35" />{display(trainer.email)}</p></div><div><p className="text-xs text-ink/45">Employee code</p><p className="mt-1 text-sm font-semibold text-ink">{display(trainer.employee_code)}</p></div><div><p className="text-xs text-ink/45">Hire date</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><CalendarDays size={15} className="text-ink/35" />{formatDate(trainer.hire_date)}</p></div><div><p className="text-xs text-ink/45">Assigned members</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><UsersRound size={15} className="text-ink/35" />{trainer.assigned_members_count ?? 0}</p></div></div><div className="mt-6 border-t border-ink/10 pt-5"><p className="text-xs text-ink/45">Biography</p><p className="mt-2 text-sm leading-6 text-ink/65">{display(trainer.biography)}</p><p className="mt-3 text-xs font-semibold text-ink/45">{trainer.experience_years ?? '—'} years of experience</p></div>{Array.isArray(trainer.assigned_members) && <div className="mt-6 border-t border-ink/10 pt-5"><p className="text-xs font-bold uppercase tracking-[0.2em] text-ink/45">Assigned members</p>{trainer.assigned_members.length ? <div className="mt-3 divide-y divide-ink/10">{trainer.assigned_members.map((member) => <div key={member.id} className="flex items-center justify-between gap-4 py-3"><div><p className="text-sm font-semibold text-ink">{display(member.name)}</p><p className="text-xs text-ink/50">{display(member.email)}</p></div><span className="text-xs font-semibold capitalize text-ink/55">{display(member.status)}</span></div>)}</div> : <p className="mt-3 text-sm text-ink/55">No members assigned.</p>}</div>}</>}</div><div className="flex shrink-0 justify-end border-t border-ink/10 bg-white px-6 py-4 sm:px-8"><Button type="button" onClick={onClose} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Close</Button></div></motion.div></AdminModal>}</AnimatePresence>
}
