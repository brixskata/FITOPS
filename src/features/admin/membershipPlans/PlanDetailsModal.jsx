import AdminModal from '../../../components/common/AdminModal'
import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { CalendarDays, Clock3, Layers3, X } from 'lucide-react'
import Button from '../../../components/common/Button'

const display = (value) => value === null || value === undefined || value === '' ? '—' : value
const formatDate = (date) => date ? new Date(date).toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' }) : '—'
const formatPrice = (price) => Number(price ?? 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })

export default function PlanDetailsModal({ open, loading, error, plan, onClose }) {
  useEffect(() => { if (!open) return undefined; const previousOverflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = previousOverflow } }, [open])
  return <AnimatePresence>{open && <AdminModal><motion.div role="dialog" aria-modal="true" aria-labelledby="plan-details-title" initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 10 }} className="flex w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Membership plan</p><h2 id="plan-details-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">Details</h2></div><button type="button" onClick={onClose} aria-label="Close Membership Plan details" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button></div><div className="min-h-0 overflow-y-auto px-6 py-6 sm:px-8">{loading ? <div className="flex min-h-60 items-center justify-center text-ink/45">Loading Membership Plan details...</div> : error ? <div className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">{error}</div> : plan && <><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-ink text-xl font-bold text-accent"><Layers3 size={25} /></div><div><h3 className="text-xl font-bold text-ink">{display(plan.name)}</h3><p className="mt-1 text-sm text-ink/55">{display(plan.slug)}</p></div><span className="inline-flex w-fit rounded-full border border-ink/10 bg-ink/5 px-3 py-1 text-xs font-bold uppercase tracking-[0.2em] text-ink/65 sm:ml-auto">{display(plan.status)}</span></div><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-xs text-ink/45">Duration</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><Clock3 size={15} className="text-ink/35" />{display(plan.duration_days)} days</p></div><div><p className="text-xs text-ink/45">Price</p><p className="mt-1 text-sm font-semibold text-ink">{formatPrice(plan.price)}</p></div><div><p className="text-xs text-ink/45">Memberships</p><p className="mt-1 text-sm font-semibold text-ink">{plan.memberships_count ?? 0}</p></div><div><p className="text-xs text-ink/45">Created</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><CalendarDays size={15} className="text-ink/35" />{formatDate(plan.created_at)}</p></div></div><div className="mt-6 border-t border-ink/10 pt-5"><p className="text-xs text-ink/45">Description</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-ink/65">{display(plan.description)}</p></div></>}</div><div className="flex shrink-0 justify-end border-t border-ink/10 bg-white px-6 py-4 sm:px-8"><Button type="button" onClick={onClose} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Close</Button></div></motion.div></AdminModal>}</AnimatePresence>
}
