import AdminModal from '../../../components/common/AdminModal'
import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { LoaderCircle, X } from 'lucide-react'
import Button from '../../../components/common/Button'

function Field({ label, name, type = 'text', value, error, onChange, required = false, min }) {
  return <label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">{label}{required && <span className="text-rose-600"> *</span>}</span><input name={name} type={type} min={min} step={type === 'number' && name === 'price' ? '0.01' : undefined} value={value ?? ''} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{error && <p className="mt-2 text-xs font-medium text-rose-600">{error[0]}</p>}</label>
}

export default function PlanModal({ open, mode, loading, form, errors, message, saving, onClose, onSubmit, onChange }) {
  const isEditing = mode === 'edit'
  useEffect(() => { if (!open) return undefined; const previousOverflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = previousOverflow } }, [open])

  return <AnimatePresence>{open && <AdminModal><motion.div role="dialog" aria-modal="true" aria-labelledby="plan-modal-title" initial={{ opacity: 0, y: 18, scale: 0.98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 10 }} transition={{ duration: 0.25 }} className="flex w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Admin workspace</p><h2 id="plan-modal-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">{isEditing ? 'Edit Plan' : 'Add Plan'}</h2></div><button type="button" onClick={onClose} aria-label="Close Membership Plan modal" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button></div><div className="min-h-0 overflow-y-auto px-6 py-6 sm:px-8">{message && <div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">{message}</div>}{loading ? <div className="flex min-h-56 items-center justify-center text-ink/45"><LoaderCircle className="animate-spin" size={24} /></div> : <form id="membership-plan-form" onSubmit={onSubmit} className="space-y-5"><div className="grid gap-4 sm:grid-cols-2"><Field label="Plan Name" name="name" value={form.name} error={errors.name} onChange={onChange} required /><Field label="Duration (days)" name="duration_days" type="number" min="1" value={form.duration_days} error={errors.duration_days} onChange={onChange} required /><Field label="Price" name="price" type="number" min="0" value={form.price} error={errors.price} onChange={onChange} required /><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Status <span className="text-rose-600">*</span></span><select name="status" value={form.status ?? 'active'} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent"><option value="active">Active</option><option value="inactive">Inactive</option></select>{errors.status && <p className="mt-2 text-xs font-medium text-rose-600">{errors.status[0]}</p>}</label></div><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Description</span><textarea name="description" rows="5" value={form.description ?? ''} onChange={onChange} className="w-full resize-y rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.description && <p className="mt-2 text-xs font-medium text-rose-600">{errors.description[0]}</p>}</label><p className="text-xs text-ink/45">A unique plan slug is generated by Laravel from the plan name.</p></form>}</div><div className="flex shrink-0 flex-col-reverse gap-3 border-t border-ink/10 bg-white px-6 py-4 sm:flex-row sm:justify-end sm:px-8"><Button type="button" onClick={onClose} disabled={saving} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Cancel</Button><Button type="submit" form="membership-plan-form" disabled={saving || loading} className="w-full sm:w-auto">{saving ? (isEditing ? 'Updating...' : 'Saving...') : isEditing ? 'Update Plan' : 'Save Plan'}</Button></div></motion.div></AdminModal>}</AnimatePresence>
}
