import AdminModal from '../../../components/common/AdminModal'
import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { LoaderCircle, X } from 'lucide-react'
import Button from '../../../components/common/Button'
import { labelForStatus, labelForType, maintenanceStatuses, maintenanceTypes } from './maintenanceUtils'

function Field({ label, name, type = 'text', value, error, onChange, required = false, min, step }) {
  return <label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">{label}{required && <span className="text-rose-600"> *</span>}</span><input name={name} type={type} min={min} step={step} value={value ?? ''} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{error && <p className="mt-2 text-xs font-medium text-rose-600">{error[0]}</p>}</label>
}

function SelectField({ label, name, value, options, labels, error, onChange, disabled = false }) {
  return <label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">{label}<span className="text-rose-600"> *</span></span><select name={name} value={value ?? ''} onChange={onChange} disabled={disabled} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent disabled:cursor-wait disabled:opacity-60">{options.map((option) => <option key={option} value={option}>{labels(option)}</option>)}</select>{error && <p className="mt-2 text-xs font-medium text-rose-600">{error[0]}</p>}</label>
}

export default function MaintenanceModal({ open, mode, form, equipment, equipmentLoading, equipmentError, errors, message, saving, onClose, onSubmit, onChange }) {
  useEffect(() => {
    if (!open) return undefined
    const originalOverflow = document.body.style.overflow
    const handleKeyDown = (event) => { if (event.key === 'Escape' && !saving) onClose() }
    document.body.style.overflow = 'hidden'
    window.addEventListener('keydown', handleKeyDown)
    return () => { document.body.style.overflow = originalOverflow; window.removeEventListener('keydown', handleKeyDown) }
  }, [open, onClose, saving])

  return <AnimatePresence>{open && <AdminModal padding="p-2 sm:p-4"><motion.div role="dialog" aria-modal="true" aria-labelledby="maintenance-modal-title" initial={{ opacity: 0, y: 18, scale: 0.98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 10 }} transition={{ duration: 0.25 }} className="flex w-full max-w-3xl min-w-0 flex-col overflow-hidden rounded-3xl border border-ink/10 bg-white text-ink shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Maintenance module</p><h2 id="maintenance-modal-title" className="mt-2 font-heading text-3xl uppercase tracking-wide">{mode === 'edit' ? 'Edit Maintenance' : 'Add Maintenance'}</h2></div><button type="button" onClick={onClose} disabled={saving} aria-label="Close maintenance modal" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink disabled:opacity-50"><X size={18} /></button></div><div className="min-h-0 flex-1 overflow-x-hidden overflow-y-auto overscroll-contain px-6 py-6 sm:px-8">{message && <div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm leading-6 text-amber-900">{message}</div>}<form id="maintenance-record-form" onSubmit={onSubmit} className="space-y-5"><div className="grid gap-4 sm:grid-cols-2"><label className="block sm:col-span-2"><span className="mb-2 block text-sm font-medium text-ink/70">Equipment<span className="text-rose-600"> *</span></span><select name="equipment_id" value={form.equipment_id ?? ''} onChange={onChange} disabled={equipmentLoading || Boolean(equipmentError)} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent disabled:cursor-wait disabled:opacity-60"><option value="">{equipmentLoading ? 'Loading Equipment...' : equipmentError ? 'Equipment unavailable' : 'Select Equipment'}</option>{equipment.map((item) => <option key={item.id} value={item.id}>{item.name} ({item.asset_code})</option>)}</select>{equipmentError && <p className="mt-2 text-xs font-medium text-rose-600">{equipmentError}</p>}{errors.equipment_id && <p className="mt-2 text-xs font-medium text-rose-600">{errors.equipment_id[0]}</p>}</label><Field label="Maintenance Date" name="maintenance_date" type="date" value={form.maintenance_date} error={errors.maintenance_date} onChange={onChange} required /><SelectField label="Maintenance Type" name="maintenance_type" value={form.maintenance_type} options={maintenanceTypes} labels={labelForType} error={errors.maintenance_type} onChange={onChange} /><Field label="Cost" name="cost" type="number" min="0" step="0.01" value={form.cost} error={errors.cost} onChange={onChange} required /><SelectField label="Status" name="status" value={form.status} options={maintenanceStatuses} labels={labelForStatus} error={errors.status} onChange={onChange} /></div><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Description<span className="text-rose-600"> *</span></span><textarea name="description" rows="5" value={form.description ?? ''} onChange={onChange} className="w-full resize-y rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.description && <p className="mt-2 text-xs font-medium text-rose-600">{errors.description[0]}</p>}</label><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Notes</span><textarea name="notes" rows="4" value={form.notes ?? ''} onChange={onChange} className="w-full resize-y rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.notes && <p className="mt-2 text-xs font-medium text-rose-600">{errors.notes[0]}</p>}</label></form></div><div className="flex shrink-0 flex-col-reverse gap-3 border-t border-ink/10 bg-white px-6 py-4 sm:flex-row sm:justify-end sm:px-8"><Button type="button" onClick={onClose} disabled={saving} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Cancel</Button><Button type="submit" form="maintenance-record-form" disabled={saving || equipmentLoading || Boolean(equipmentError)} className="w-full sm:w-auto">{saving ? 'Saving...' : mode === 'edit' ? 'Save Changes' : 'Add Maintenance'}</Button></div></motion.div></AdminModal>}</AnimatePresence>
}
