import AdminModal from '../../../components/common/AdminModal'
import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { LoaderCircle, X } from 'lucide-react'
import Button from '../../../components/common/Button'

const money = (value) => `₱${Number(value ?? 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`
const dateTimeInput = (value) => value ? new Date(value).toISOString().slice(0, 16) : ''

export default function PaymentModal({ open, form, memberships, membershipsLoading, balance, balanceLoading, errors, message, saving, onClose, onSubmit, onChange }) {
  const selectedMembership = memberships.find((membership) => String(membership.id) === String(form.membership_id))
  useEffect(() => { if (!open) return undefined; const overflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = overflow } }, [open])
  return <AnimatePresence>{open && <AdminModal><motion.div role="dialog" aria-modal="true" aria-labelledby="payment-modal-title" initial={{ opacity: 0, y: 18, scale: 0.98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 10 }} transition={{ duration: 0.25 }} className="flex w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Admin workspace</p><h2 id="payment-modal-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">Add Payment</h2></div><button type="button" onClick={onClose} aria-label="Close Payment modal" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button></div><div className="min-h-0 overflow-y-auto px-6 py-6 sm:px-8">{message && <div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">{message}</div>}<form id="payment-form" onSubmit={onSubmit} className="space-y-5"><div className="grid gap-4 sm:grid-cols-2"><label className="block sm:col-span-2"><span className="mb-2 block text-sm font-medium text-ink/70">Membership <span className="text-rose-600">*</span></span><select name="membership_id" value={form.membership_id ?? ''} onChange={onChange} disabled={membershipsLoading} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent disabled:cursor-wait disabled:opacity-60"><option value="">{membershipsLoading ? 'Loading memberships...' : memberships.length ? 'Select a membership' : 'No memberships available for payment'}</option>{memberships.map((membership) => <option key={membership.id} value={membership.id}>{membership.member?.name ?? 'Member'} — {membership.membership_number} — {membership.membership_plan?.name ?? 'Plan'}</option>)}</select>{errors.membership_id && <p className="mt-2 text-xs font-medium text-rose-600">{errors.membership_id[0]}</p>}</label></div>{selectedMembership && <div className="grid gap-3 rounded-2xl border border-accent/40 bg-accent/10 p-4 text-sm sm:grid-cols-3"><div><p className="text-xs text-ink/55">Membership Price</p><p className="mt-1 font-bold text-ink">{balanceLoading ? 'Loading...' : money(balance?.membership_price)}</p></div><div><p className="text-xs text-ink/55">Current Paid</p><p className="mt-1 font-bold text-ink">{balanceLoading ? 'Loading...' : money(balance?.paid_total)}</p></div><div><p className="text-xs text-ink/55">Outstanding</p><p className="mt-1 font-bold text-ink">{balanceLoading ? 'Loading...' : money(balance?.outstanding_balance)}</p></div><p className="text-xs leading-5 text-ink/60 sm:col-span-3">These values come from Laravel and are informational. The server remains authoritative when this payment is submitted.</p></div>}<div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Amount <span className="text-rose-600">*</span></span><input name="amount" type="number" min="0.01" step="0.01" value={form.amount ?? ''} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.amount && <p className="mt-2 text-xs font-medium text-rose-600">{errors.amount[0]}</p>}</label><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Payment Method <span className="text-rose-600">*</span></span><select name="payment_method" value={form.payment_method ?? 'cash'} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent"><option value="cash">Cash</option><option value="gcash">GCash</option><option value="maya">Maya</option><option value="card">Card</option></select>{errors.payment_method && <p className="mt-2 text-xs font-medium text-rose-600">{errors.payment_method[0]}</p>}</label><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Paid At <span className="text-rose-600">*</span></span><input name="paid_at" type="datetime-local" value={dateTimeInput(form.paid_at)} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.paid_at && <p className="mt-2 text-xs font-medium text-rose-600">{errors.paid_at[0]}</p>}</label><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Status <span className="text-rose-600">*</span></span><select name="status" value={form.status ?? 'pending'} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent"><option value="pending">Pending</option><option value="paid">Paid</option><option value="failed">Failed</option><option value="refunded">Refunded</option></select>{errors.status && <p className="mt-2 text-xs font-medium text-rose-600">{errors.status[0]}</p>}</label></div><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Reference Number</span><input name="reference_number" value={form.reference_number ?? ''} onChange={onChange} placeholder="Optional" className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.reference_number && <p className="mt-2 text-xs font-medium text-rose-600">{errors.reference_number[0]}</p>}</label><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Notes</span><textarea name="notes" rows="4" value={form.notes ?? ''} onChange={onChange} className="w-full resize-y rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.notes && <p className="mt-2 text-xs font-medium text-rose-600">{errors.notes[0]}</p>}</label></form></div><div className="flex shrink-0 flex-col-reverse gap-3 border-t border-ink/10 bg-white px-6 py-4 sm:flex-row sm:justify-end sm:px-8"><Button type="button" onClick={onClose} disabled={saving} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Cancel</Button><Button type="submit" form="payment-form" disabled={saving || membershipsLoading || balanceLoading || !memberships.length} className="w-full sm:w-auto">{saving ? 'Saving...' : 'Save Payment'}</Button></div></motion.div></AdminModal>}</AnimatePresence>
}
