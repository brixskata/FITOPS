import AdminModal from '../../../components/common/AdminModal'
import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { CalendarDays, CreditCard, FileText, UserRound, X } from 'lucide-react'
import Button from '../../../components/common/Button'

const date = (value) => value ? new Date(value).toLocaleString(undefined, { dateStyle: 'medium', timeStyle: 'short' }) : '—'
const money = (value) => `₱${Number(value ?? 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`
const label = (value) => value ? value.charAt(0).toUpperCase() + value.slice(1) : '—'

export default function PaymentDetailsModal({ open, loading, error, payment, onClose }) {
  useEffect(() => { if (!open) return undefined; const overflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = overflow } }, [open])
  return <AnimatePresence>{open && <AdminModal><motion.div role="dialog" aria-modal="true" aria-labelledby="payment-details-title" initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 10 }} className="flex w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Payment</p><h2 id="payment-details-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">Details</h2></div><button type="button" onClick={onClose} aria-label="Close Payment details" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button></div><div className="min-h-0 overflow-y-auto px-6 py-6 sm:px-8">{loading ? <div className="flex min-h-60 items-center justify-center text-ink/45"><span className="flex items-center gap-3"><CreditCard className="animate-pulse" size={20} /> Loading payment details...</span></div> : error ? <div className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">{error}</div> : payment && <><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-ink text-accent"><CreditCard size={25} /></div><div><h3 className="font-mono text-lg font-bold text-ink">{payment.receipt_number}</h3><p className="mt-1 text-sm text-ink/55">{payment.member?.name ?? '—'} · {payment.membership_plan?.name ?? '—'}</p></div><span className="inline-flex w-fit rounded-full border border-ink/10 bg-ink/5 px-3 py-1 text-xs font-bold uppercase tracking-[0.2em] text-ink/65 sm:ml-auto">{label(payment.status)}</span></div><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-3"><div><p className="text-xs text-ink/45">Amount</p><p className="mt-1 text-sm font-semibold text-ink">{money(payment.amount)}</p></div><div><p className="text-xs text-ink/45">Payment Method</p><p className="mt-1 text-sm font-semibold text-ink">{label(payment.payment_method)}</p></div><div><p className="text-xs text-ink/45">Reference Number</p><p className="mt-1 text-sm font-semibold text-ink">{payment.reference_number || '—'}</p></div><div><p className="text-xs text-ink/45">Paid At</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><CalendarDays size={15} className="text-ink/35" />{date(payment.paid_at)}</p></div><div><p className="text-xs text-ink/45">Member</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><UserRound size={15} className="text-ink/35" />{payment.member?.name ?? '—'}</p><p className="mt-1 text-xs text-ink/55">{payment.member?.email ?? '—'}</p></div><div><p className="text-xs text-ink/45">Plan</p><p className="mt-1 text-sm font-semibold text-ink">{payment.membership_plan?.name ?? '—'}</p></div></div><div className="mt-7 rounded-2xl border border-ink/10 bg-ink/[0.02] p-4"><p className="text-xs font-bold uppercase tracking-[0.2em] text-ink/45">Membership</p><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-xs text-ink/45">Number</p><p className="mt-1 font-mono text-xs font-semibold text-ink">{payment.membership?.membership_number ?? '—'}</p></div><div><p className="text-xs text-ink/45">Price</p><p className="mt-1 text-sm font-semibold text-ink">{money(payment.membership_price)}</p></div><div><p className="text-xs text-ink/45">Start</p><p className="mt-1 text-sm font-semibold text-ink">{date(payment.membership?.starts_at)}</p></div><div><p className="text-xs text-ink/45">End</p><p className="mt-1 text-sm font-semibold text-ink">{date(payment.membership?.ends_at)}</p></div></div><p className="mt-3 text-xs text-ink/55">Membership status: <span className="font-semibold text-ink">{label(payment.membership?.status)}</span></p></div><div className="mt-5 grid gap-4 rounded-2xl border border-accent/40 bg-accent/10 p-4 sm:grid-cols-3"><div><p className="text-xs text-ink/55">Membership Price</p><p className="mt-1 font-bold text-ink">{money(payment.membership_price)}</p></div><div><p className="text-xs text-ink/55">Paid Total</p><p className="mt-1 font-bold text-ink">{money(payment.paid_total)}</p></div><div><p className="text-xs text-ink/55">Outstanding Balance</p><p className="mt-1 font-bold text-ink">{money(payment.outstanding_balance)}</p></div></div><div className="mt-5 border-t border-ink/10 pt-5"><p className="flex items-center gap-2 text-xs text-ink/45"><FileText size={14} /> Notes</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-ink/65">{payment.notes || '—'}</p></div></>}</div><div className="flex shrink-0 justify-end border-t border-ink/10 bg-white px-6 py-4 sm:px-8"><Button type="button" onClick={onClose} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Close</Button></div></motion.div></AdminModal>}</AnimatePresence>
}
