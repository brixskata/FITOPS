import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { Clock3, LogOut, UserRound, X } from 'lucide-react'
import Button from '../../../components/common/Button'
import { formatAttendanceDateTime, formatDuration } from './attendanceUtils'

export default function AttendanceDetailsModal({ open, loading, error, attendance, onClose, onRetry, onCheckOut }) {
  useEffect(() => {
    if (!open) return undefined
    const overflow = document.body.style.overflow
    const onKeyDown = (event) => { if (event.key === 'Escape') onClose() }
    document.body.style.overflow = 'hidden'
    window.addEventListener('keydown', onKeyDown)
    return () => { document.body.style.overflow = overflow; window.removeEventListener('keydown', onKeyDown) }
  }, [onClose, open])

  return <AnimatePresence>{open && <motion.div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/60 p-4 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.div role="dialog" aria-modal="true" aria-labelledby="attendance-details-title" initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 10 }} className="flex max-h-[90vh] w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Attendance record</p><h2 id="attendance-details-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">Details</h2></div><button type="button" onClick={onClose} aria-label="Close Attendance details" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button></div><div className="min-h-0 overflow-y-auto px-6 py-6 sm:px-8">{loading ? <div className="flex min-h-60 items-center justify-center text-ink/45"><span className="flex items-center gap-3"><Clock3 className="animate-pulse" size={20} /> Loading Attendance details...</span></div> : error ? <div className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-4 text-sm text-amber-900"><p>{error}</p><button type="button" onClick={onRetry} className="mt-3 rounded-full border border-amber-300 px-4 py-2 text-xs font-bold uppercase tracking-wider">Retry</button></div> : attendance && <><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-ink text-accent"><UserRound size={25} /></div><div><h3 className="text-lg font-bold text-ink">{attendance.member?.name ?? 'Member unavailable'}</h3><p className="mt-1 text-sm text-ink/55">{attendance.member?.email ?? '—'}</p></div><span className={`inline-flex w-fit rounded-full border px-3 py-1 text-xs font-bold uppercase tracking-[0.18em] sm:ml-auto ${attendance.session_state === 'open' ? 'border-accent/50 bg-accent/15 text-ink' : 'border-emerald-500/20 bg-emerald-500/10 text-emerald-700'}`}>{attendance.session_state === 'open' ? 'Currently Checked In' : 'Completed'}</span></div><div className="mt-7 grid gap-5 rounded-2xl border border-ink/10 bg-ink/[0.02] p-5 sm:grid-cols-2"><div><p className="text-xs text-ink/45">Check In</p><p className="mt-1 text-sm font-semibold text-ink">{formatAttendanceDateTime(attendance.checked_in_at)}</p></div><div><p className="text-xs text-ink/45">Check Out</p><p className="mt-1 text-sm font-semibold text-ink">{attendance.session_state === 'open' ? 'Currently checked in' : formatAttendanceDateTime(attendance.checked_out_at)}</p></div><div><p className="text-xs text-ink/45">Duration</p><p className="mt-1 text-sm font-semibold text-ink">{attendance.session_state === 'open' ? '—' : formatDuration(attendance.duration)}</p></div><div><p className="text-xs text-ink/45">Recorded By</p><p className="mt-1 text-sm font-semibold text-ink">{attendance.recorded_by?.name ?? 'System'}</p></div><div><p className="text-xs text-ink/45">Created At</p><p className="mt-1 text-sm font-semibold text-ink">{formatAttendanceDateTime(attendance.created_at)}</p></div><div><p className="text-xs text-ink/45">Session State</p><p className="mt-1 text-sm font-semibold capitalize text-ink">{attendance.session_state}</p></div></div><div className="mt-5 border-t border-ink/10 pt-5"><p className="text-xs font-bold uppercase tracking-[0.2em] text-ink/45">Notes</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-ink/65">{attendance.notes || 'No notes recorded.'}</p></div></>}</div><div className="flex shrink-0 flex-col-reverse gap-3 border-t border-ink/10 bg-white px-6 py-4 sm:flex-row sm:justify-end sm:px-8"><Button type="button" onClick={onClose} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Close</Button>{attendance?.session_state === 'open' && !loading && !error && <Button type="button" onClick={() => onCheckOut(attendance)} className="w-full sm:w-auto"><LogOut size={16} /> Check Out</Button>}</div></motion.div></motion.div>}</AnimatePresence>
}
