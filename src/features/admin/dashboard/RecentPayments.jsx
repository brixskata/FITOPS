import { ArrowUpRight, CreditCard } from 'lucide-react'
import { Link } from 'react-router-dom'
import { formatCurrency, formatDateTime, humanize, statusClass } from './dashboardUtils'

export default function RecentPayments({ payments = [] }) {
  return <section className="rounded-3xl border border-ink/10 bg-white p-6 shadow-[0_18px_60px_rgba(18,18,18,0.05)] sm:p-7"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-accent"><CreditCard size={18} /></span><h2 className="font-heading text-2xl uppercase text-ink">Recent Payments</h2></div><Link to="/admin/payments" className="inline-flex items-center gap-1 text-xs font-bold uppercase tracking-wider text-ink/50 transition hover:text-ink">View all <ArrowUpRight size={14} /></Link></div>{payments.length ? <div className="mt-6 divide-y divide-ink/10">{payments.map((payment) => <div key={payment.id} className="flex items-center gap-3 py-4 first:pt-0 last:pb-0"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-ink text-accent"><CreditCard size={16} /></div><div className="min-w-0 flex-1"><p className="truncate font-mono text-xs font-bold text-ink">{payment.receipt_number || 'Receipt unavailable'}</p><p className="mt-1 text-xs text-ink/45">{humanize(payment.payment_method)} · {formatDateTime(payment.paid_at)}</p></div><div className="text-right"><p className="text-sm font-bold text-ink">{formatCurrency(payment.amount)}</p><span className={`mt-1 inline-flex rounded-full border px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider ${statusClass(payment.status)}`}>{humanize(payment.status)}</span></div></div>)}</div> : <div className="mt-8 rounded-2xl border border-dashed border-ink/15 px-4 py-8 text-center text-sm text-ink/50">No recent payments.</div>}</section>
}
