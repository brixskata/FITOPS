import { Edit3, Eye, RefreshCw } from 'lucide-react'
import { motion } from 'framer-motion'

const statusStyles = { active: 'border-emerald-500/20 bg-emerald-500/10 text-emerald-700', expired: 'border-amber-500/20 bg-amber-500/10 text-amber-800', cancelled: 'border-rose-500/20 bg-rose-500/10 text-rose-700' }
const date = (value) => value ? new Date(value).toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' }) : '—'
const price = (value) => `₱${Number(value ?? 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`

export default function MembershipTable({ memberships, onView, onEdit, onRenew, onStatusChange, renewingId, statusUpdatingId }) {
  return <div className="overflow-hidden rounded-3xl border border-ink/10 bg-white shadow-[0_18px_60px_rgba(18,18,18,0.06)]"><div className="overflow-x-auto"><table className="min-w-[1180px] w-full border-separate border-spacing-0"><thead className="bg-[#fbfbf9]"><tr>{['Member', 'Membership Number', 'Plan', 'Start Date', 'End Date', 'Price', 'Status', 'Payments', 'Actions'].map((column) => <th key={column} className="px-4 py-4 text-left text-[11px] font-bold uppercase tracking-[0.22em] text-ink/45">{column}</th>)}</tr></thead><tbody>{memberships.map((membership, index) => { const pending = renewingId === membership.id || statusUpdatingId === membership.id; return <motion.tr key={membership.id} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.3, delay: Math.min(index * 0.04, 0.2) }} className="border-t border-ink/5 text-sm text-ink/80 hover:bg-ink/[0.02]"><td className="px-4 py-4"><p className="font-semibold text-ink">{membership.member?.name ?? '—'}</p><p className="mt-1 text-xs text-ink/45">{membership.member?.email ?? '—'}</p></td><td className="px-4 py-4 font-mono text-xs font-semibold text-ink">{membership.membership_number}</td><td className="px-4 py-4"><p className="font-semibold text-ink">{membership.membership_plan?.name ?? '—'}</p><p className="mt-1 text-xs text-ink/45">{membership.membership_plan?.duration_days ?? '—'} days</p></td><td className="px-4 py-4 whitespace-nowrap">{date(membership.starts_at)}</td><td className="px-4 py-4 whitespace-nowrap">{date(membership.ends_at)}</td><td className="px-4 py-4 whitespace-nowrap font-semibold text-ink">{price(membership.price)}</td><td className="px-4 py-4"><select value={membership.status} disabled={pending} onChange={(event) => onStatusChange(membership, event.target.value)} aria-label={`Change status for ${membership.membership_number}`} className={`rounded-full border px-3 py-1 text-xs font-bold uppercase tracking-[0.16em] outline-none transition ${statusStyles[membership.status] || 'border-ink/10 bg-ink/5 text-ink/60'} disabled:cursor-not-allowed disabled:opacity-50`}><option value="active">Active</option><option value="expired">Expired</option><option value="cancelled">Cancelled</option></select></td><td className="px-4 py-4 text-center font-semibold text-ink">{membership.payments_count ?? 0}</td><td className="px-4 py-4"><div className="flex items-center justify-end gap-2"><button type="button" onClick={() => onView(membership)} aria-label={`View ${membership.membership_number}`} className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><Eye size={16} /></button><button type="button" onClick={() => onEdit(membership)} disabled={pending} aria-label={`Edit ${membership.membership_number}`} className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink disabled:cursor-not-allowed disabled:opacity-40"><Edit3 size={16} /></button><button type="button" onClick={() => onRenew(membership)} disabled={pending} aria-label={`Renew ${membership.membership_number}`} className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink disabled:cursor-not-allowed disabled:opacity-40"><RefreshCw size={16} className={renewingId === membership.id ? 'animate-spin' : ''} /></button></div></td></motion.tr> })}</tbody></table></div></div>
}
