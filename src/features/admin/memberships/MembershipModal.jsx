import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { LoaderCircle, X } from 'lucide-react'
import Button from '../../../components/common/Button'

const toDateInput = (value) => value ? new Date(value).toISOString().slice(0, 10) : ''
const formatPrice = (value) => `₱${Number(value ?? 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`

export default function MembershipModal({ open, mode, loading, optionsLoading, options, form, errors, message, saving, onClose, onSubmit, onChange }) {
  const editing = mode === 'edit'
  const selectedPlan = options.plans.find((plan) => String(plan.id) === String(form.membership_plan_id))
  useEffect(() => { if (!open) return undefined; const overflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = overflow } }, [open])
  return <AnimatePresence>{open && <motion.div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/60 p-4 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.div role="dialog" aria-modal="true" aria-labelledby="membership-modal-title" initial={{ opacity: 0, y: 18, scale: 0.98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 10 }} transition={{ duration: 0.25 }} className="flex max-h-[90vh] w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Admin workspace</p><h2 id="membership-modal-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">{editing ? 'Edit Membership' : 'Add Membership'}</h2></div><button type="button" onClick={onClose} aria-label="Close Membership modal" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button></div><div className="min-h-0 overflow-y-auto px-6 py-6 sm:px-8">{message && <div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">{message}</div>}{loading ? <div className="flex min-h-60 items-center justify-center text-ink/45"><LoaderCircle className="animate-spin" size={24} /></div> : <form id="membership-form" onSubmit={onSubmit} className="space-y-5">{editing ? <div className="grid gap-4 rounded-2xl border border-ink/10 bg-ink/[0.02] p-4 sm:grid-cols-2"><div><p className="text-xs text-ink/45">Member</p><p className="mt-1 font-semibold text-ink">{form.member_name || '—'}</p><p className="mt-1 text-xs text-ink/50">{form.member_email || '—'}</p></div><div><p className="text-xs text-ink/45">Membership Plan</p><p className="mt-1 font-semibold text-ink">{form.plan_name || '—'}</p><p className="mt-1 text-xs text-ink/50">{form.membership_number || '—'} · {formatPrice(form.price)}</p></div></div> : <div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Member <span className="text-rose-600">*</span></span><select name="member_id" value={form.member_id ?? ''} onChange={onChange} disabled={optionsLoading} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent disabled:cursor-wait disabled:opacity-60"><option value="">{optionsLoading ? 'Loading members...' : options.members.length ? 'Select a member' : 'No members available'}</option>{options.members.map((member) => <option key={member.id} value={member.id}>{member.name} — {member.email}</option>)}</select>{errors.member_id && <p className="mt-2 text-xs font-medium text-rose-600">{errors.member_id[0]}</p>}</label><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Membership Plan <span className="text-rose-600">*</span></span><select name="membership_plan_id" value={form.membership_plan_id ?? ''} onChange={onChange} disabled={optionsLoading} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent disabled:cursor-wait disabled:opacity-60"><option value="">{optionsLoading ? 'Loading active plans...' : options.plans.length ? 'Select an active plan' : 'No active membership plans available'}</option>{options.plans.map((plan) => <option key={plan.id} value={plan.id}>{plan.name}</option>)}</select>{errors.membership_plan_id && <p className="mt-2 text-xs font-medium text-rose-600">{errors.membership_plan_id[0]}</p>}</label></div>} {!editing && selectedPlan && <div className="grid gap-3 rounded-2xl border border-accent/40 bg-accent/10 p-4 text-sm sm:grid-cols-2"><p><span className="text-ink/55">Current price</span><span className="ml-2 font-bold text-ink">{formatPrice(selectedPlan.price)}</span></p><p><span className="text-ink/55">Duration</span><span className="ml-2 font-bold text-ink">{selectedPlan.duration_days} days</span></p><p className="sm:col-span-2 text-xs leading-5 text-ink/60">Laravel generates the membership number, price snapshot, and end date when this membership is saved.</p></div>}<div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-sm font-medium text-ink/70">Start Date <span className="text-rose-600">*</span></span><input name="starts_at" type="date" value={toDateInput(form.starts_at)} onChange={onChange} className="w-full rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink outline-none transition focus:border-accent" />{errors.starts_at && <p className="mt-2 text-xs font-medium text-rose-600">{errors.starts_at[0]}</p>}</label><label className="flex min-h-[52px] cursor-pointer items-center gap-3 self-end rounded-2xl border border-ink/10 bg-white px-4 py-3 text-sm text-ink"><input name="auto_renew" type="checkbox" checked={Boolean(form.auto_renew)} onChange={onChange} className="h-4 w-4 accent-[#FFD400]" /><span><span className="block font-medium">Auto renew</span><span className="block text-xs text-ink/50">Keep this preference for future renewals.</span></span></label></div>{errors.auto_renew && <p className="text-xs font-medium text-rose-600">{errors.auto_renew[0]}</p>}</form>}</div><div className="flex shrink-0 flex-col-reverse gap-3 border-t border-ink/10 bg-white px-6 py-4 sm:flex-row sm:justify-end sm:px-8"><Button type="button" onClick={onClose} disabled={saving} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Cancel</Button><Button type="submit" form="membership-form" disabled={saving || loading || (!editing && (optionsLoading || !options.members.length || !options.plans.length))} className="w-full sm:w-auto">{saving ? (editing ? 'Updating...' : 'Saving...') : editing ? 'Update Membership' : 'Save Membership'}</Button></div></motion.div></motion.div>}</AnimatePresence>
}
