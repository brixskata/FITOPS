import { useEffect } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { CalendarDays, CreditCard, RefreshCw, UserRound, X } from 'lucide-react'
import Button from '../../../components/common/Button'

const date = (value) => value ? new Date(value).toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' }) : '—'
const price = (value) => `₱${Number(value ?? 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`

export default function MembershipDetailsModal({ open, loading, error, membership, onClose }) {
  useEffect(() => { if (!open) return undefined; const overflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = overflow } }, [open])
  return <AnimatePresence>{open && <motion.div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/60 p-4 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.div role="dialog" aria-modal="true" aria-labelledby="membership-details-title" initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 10 }} className="flex max-h-[90vh] w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex shrink-0 items-start justify-between border-b border-ink/10 px-6 py-5 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-ink/45">Membership</p><h2 id="membership-details-title" className="mt-2 font-heading text-3xl uppercase tracking-wide text-ink">Details</h2></div><button type="button" onClick={onClose} aria-label="Close Membership details" className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-ink/10 text-ink/60 transition hover:border-accent hover:text-ink"><X size={18} /></button></div><div className="min-h-0 max-h-[calc(90vh-176px)] overflow-y-auto px-6 py-6 sm:px-8">{loading ? <div className="flex min-h-60 items-center justify-center text-ink/45">Loading membership details...</div> : error ? <div className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">{error}</div> : membership && <><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-ink text-accent"><CreditCard size={25} /></div><div><h3 className="font-mono text-lg font-bold text-ink">{membership.membership_number}</h3><p className="mt-1 text-sm text-ink/55">{membership.membership_plan?.name ?? '—'}</p></div><span className="inline-flex w-fit rounded-full border border-ink/10 bg-ink/5 px-3 py-1 text-xs font-bold uppercase tracking-[0.2em] text-ink/65 sm:ml-auto">{membership.status}</span></div><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-3"><div><p className="text-xs text-ink/45">Member</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><UserRound size={15} className="text-ink/35" />{membership.member?.name ?? '—'}</p><p className="mt-1 text-xs text-ink/55">{membership.member?.email ?? '—'}</p></div><div><p className="text-xs text-ink/45">Membership Plan</p><p className="mt-1 text-sm font-semibold text-ink">{membership.membership_plan?.name ?? '—'}</p><p className="mt-1 text-xs text-ink/55">{membership.membership_plan?.duration_days ?? '—'} days</p></div><div><p className="text-xs text-ink/45">Price Snapshot</p><p className="mt-1 text-sm font-semibold text-ink">{price(membership.price)}</p></div><div><p className="text-xs text-ink/45">Start Date</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><CalendarDays size={15} className="text-ink/35" />{date(membership.starts_at)}</p></div><div><p className="text-xs text-ink/45">End Date</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><CalendarDays size={15} className="text-ink/35" />{date(membership.ends_at)}</p></div><div><p className="text-xs text-ink/45">Payments</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><CreditCard size={15} className="text-ink/35" />{membership.payments_count ?? 0}</p></div><div><p className="text-xs text-ink/45">Auto Renew</p><p className="mt-1 flex items-center gap-2 text-sm font-semibold text-ink"><RefreshCw size={15} className="text-ink/35" />{membership.auto_renew ? 'Enabled' : 'Disabled'}</p></div></div></>}</div><div className="flex shrink-0 justify-end border-t border-ink/10 bg-white px-6 py-4 sm:px-8"><Button type="button" onClick={onClose} className="w-full bg-white text-ink hover:bg-accent sm:w-auto">Close</Button></div></motion.div></motion.div>}</AnimatePresence>
}
